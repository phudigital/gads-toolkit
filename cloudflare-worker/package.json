{
  "name": "gads-central-service",
  "version": "4.1.3",
  "type": "module",
  "description": "GAds Toolkit Central Service — Cloudflare Worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npm run check && wrangler deploy",
    "tail": "wrangler tail",
    "check:version": "node scripts/check-version.mjs",
    "check": "npm run check:version && node --check src/index.js && node --check src/api.js && node --check src/admin.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
